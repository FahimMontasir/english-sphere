# Server environment

Server-only environment access. Bun loads `.env` files and values are read from `Bun.env` without a
runtime schema dependency. Mobile environment variables do not belong in this package.
